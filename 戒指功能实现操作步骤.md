# Armory Ring 戒指功能实现操作步骤

目标：你先不管机械臂复杂动作，只负责让戒指稳定输出 3 个可识别指令，再交给树莓派和机械臂同学联动。

## 0. 你的第一阶段交付物

- 能在电脑终端看到戒指手势事件。
- 能生成 `ring_events.jsonl`，每一行是一条 JSON 指令。
- 三个手势先映射成三种义手模式：
  - `rotate_back` -> `winter_soldier` -> `grip`
  - `rotate_front` -> `shinobi` -> `switch_tool`
  - `wave` -> `devil_breaker` -> `burst`

## 1. 准备硬件

1. 戒指充电。
2. 打开 Windows 蓝牙。
3. 手机上的戒指 App 先断开连接，避免电脑连不上。
4. 戒指靠近电脑。

## 2. 打开 PowerShell

进入 SDK 文件夹：

```powershell
cd "C:\Users\张朵朵\Desktop\ring\ring_sound_SDK"
```

确认 Python 版本：

```powershell
python --version
```

当前电脑已经是 Python 3.12.9，版本够用。

安装蓝牙依赖：

```powershell
python -m pip install bleak
```

## 3. 扫描戒指蓝牙地址

```powershell
python ring_sound.py scan
```

终端会出现类似：

```text
name='xxx' address=F1:C1:8A:35:40:FB rssi=-60
```

记下 `address=` 后面的地址。下面都用 `<你的戒指地址>` 表示。

## 4. 验证能连上戒指

```powershell
python ring_sound.py info --address <你的戒指地址>
```

能看到电量、固件版本、型号，就说明连接成功。

## 5. 运行手势监听脚本

```powershell
python armory_ring_gesture_demo.py --address <你的戒指地址>
```

操作戒指：

1. 先单击一次戒指，尝试切到手势模式。
2. 长按戒指。
3. 做一个动作：向后旋、向前旋、挥动。
4. 松手。

终端应该输出类似：

```text
[2026-07-23T16:20:00] gesture=wave mode=devil_breaker arm_action=burst
```

同时文件夹里会生成：

```text
ring_events.jsonl
```

这个文件就是你给树莓派同学的第一版接口。

## 6. 和树莓派同学的接口约定

你先给他这三种动作名：

```json
{"gesture":"rotate_back","mode":"winter_soldier","arm_action":"grip"}
{"gesture":"rotate_front","mode":"shinobi","arm_action":"switch_tool"}
{"gesture":"wave","mode":"devil_breaker","arm_action":"burst"}
```

后面你们再把 `ring_events.jsonl` 改成 HTTP 发送：

```text
POST http://树莓派IP:8000/ring-event
```

## 7. 常见问题

- `Package(s) not found: bleak`：运行 `python -m pip install bleak`。
- 扫不到戒指：关闭手机 App 蓝牙连接，戒指靠近电脑，重新运行 scan。
- `Device busy`：戒指可能还在录音模式，单击切换后再试。
- 没有手势输出：先长按，做动作，再松手；不要只挥一下。
- 比赛现场不稳：保留键盘热键兜底，让策划同学演示时能切换模式。

## 8. 你今天只需要完成的事

1. 扫到戒指地址。
2. `info` 命令成功。
3. `armory_ring_gesture_demo.py` 能打印三种手势。
4. 把 `ring_events.jsonl` 发给树莓派同学看格式。

先做到这里，你的戒指模块就已经从“想法”变成“可联动输入设备”了。
